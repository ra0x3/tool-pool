version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: wasi-multi-postgres
    environment:
      POSTGRES_DB: todos_db
      POSTGRES_USER: wasi_user
      POSTGRES_PASSWORD: wasi_password
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wasi_user -d todos_db"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data: