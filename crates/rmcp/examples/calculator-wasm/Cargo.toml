[package]
name = "calculator-wasm"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Optimize for size since this will be a WASM module
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true